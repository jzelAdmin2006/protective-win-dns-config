# Protective Windows DNS config

This is a setup which applies the DNS servers by Cloudflare's 1.1.1.1 that proactively block malware and adult content for every network adapter on a Windows system.

## How to use

- Clone repository
- Run Install.ps1